/** Matches product default company gross margin (20%). DB column default may still be 25 until a later migration. */
export const DEFAULT_MARGIN_PERCENT = 20;

/** Quality/spec multiplier does not apply to non-finish-dependent labour or materials. */
export const NO_FINISH_QUALITY_FACTOR = 1;
