/**
 * Stage 3.1B.2 — Scope relationship catalogue version.
 * Bump when relationship meaning, condition semantics, canonical IDs,
 * suppression logic, or requirement levels change.
 * Adding relationships that can alter discovery output also requires a bump.
 * Do not couple to AI model versions.
 */
export const SCOPE_RELATIONSHIP_CATALOGUE_VERSION =
  "scope-relationship-catalogue/v1" as const;
