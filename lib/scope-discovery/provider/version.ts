/**
 * Stage 3.1B.3 — Scope discovery AI prompt version.
 * Bump when instructions, evidence rules, deterministic-authority wording,
 * or required schema shape change. Never use model name as prompt version.
 * Independent of Analyse Job BRIEF_EXTRACTION_SYSTEM_PROMPT.
 */
export const SCOPE_DISCOVERY_PROMPT_VERSION =
  "scope-discovery-prompt/v1" as const;

export const SCOPE_DISCOVERY_PROVIDER_NAME = "anthropic" as const;
